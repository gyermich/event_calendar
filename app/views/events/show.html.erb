<%= @event.name %></br>
<%= @event.url %></br>
<%= @event.date %></br>
<% user = User.find_by_id(@event.user_id) %>
By <%= "#{user.name}"%>:
<%= button_to "Join", join_path(@event) %>
Going:
<% @event.users.each do |user| %>
  <%= "#{user.name}" %>
  <% end%>
